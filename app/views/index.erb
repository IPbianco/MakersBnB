
<script id="property-template" type="text/x-handlebars-template">
  <div class="album text-muted">
    <div class="container">
      <div class="row">
        {{#each rentals}}
        <div class="card">
          <a class="link" name="{{id}}" href="/rentals/view?id={{id}}">
            <img src={{image}} alt="Mountain View" width='356' height='500'>
          </a>
          <div class="card-text">
            <p>Address: {{address}}</p>
            <p>Price/night: £ {{price}}</p>
          </div>
        </div>
        {{/each}}
      </div>
    </div>
  </div>
</script>
<script src='/js/interface.js'></script>
